---
"@openqueue/cli": patch
"@openqueue/sdk": patch
"@openqueue/worker": patch
"@openqueue/workbench": patch
---

Fix published package metadata so npm consumers can install OpenQueue without workspace resolution, and expose the supported Workbench React UI entrypoint plus stylesheet.
