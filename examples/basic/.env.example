REDIS_URL=redis://localhost:6379
DATABASE_URL=postgres://openqueue:openqueue@localhost:5432/openqueue
PORT=8090
OPENQUEUE_NAMESPACE=openqueue-basic
# OPENQUEUE_API_TOKEN=dev-token
# `bun run dev` provisions the demo's Postgres schema (openqueue_demo) for you;
# run `bun run db:push` on its own to (re)provision without booting the worker.
